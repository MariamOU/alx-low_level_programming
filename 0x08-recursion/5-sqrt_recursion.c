#include<stdio.h>
#include "main.h"
/**
 * _sqrt_recursion - the entry function
 * @n: a number
 * Description: 'Program uses the function _sqrt_recursion'
 * Return: the natural square root of a number or -1 if n has no sr
 */
int _sqrt_recursion(int n)
{
if (n < 0)
{
return (-1);
}
if (n == 0 || n == 1)
{
return (n);
}
}
