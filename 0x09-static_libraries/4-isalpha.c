#include<stdio.h>
#include "main.h"
/**
 * _isalpha - the entry function
 * @c: the character that is checked
 * Description: 'Program uses _isalpha()'
 * Return: 0
 */
int _isalpha(int c)
{
return (0);
}
