#include<stdio.h>
#include "main.h"
/**
 * _memset - the entry function
 * @s: the first character that is checked
 * @b: the second character that is checked
 * @n: the third character that is checked
 * Description: 'Program uses _memset()'
 * Return: 0
 */
char *_memset(char *s, char b, unsigned int n)
{
return (0);
}
