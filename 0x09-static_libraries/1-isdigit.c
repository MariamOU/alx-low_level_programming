#include<stdio.h>
#include "main.h"
/**
 * _isdigit - the entry function
 * @c: the character that is checked
 * Description: 'Program uses _isdigit()'
 * Return: 0
 */
int _isdigit(int c)
{
return (0);
}
