#include<stdio.h>
#include "main.h"
/**
 * _strchr - the entry function
 * @s: the first character that is checked
 * @c: the second character that is checked
 * Description: 'Program uses _strchr()'
 * Return: 0
 */
char *_strchr(char *s, char c)
{
return (0);
}
