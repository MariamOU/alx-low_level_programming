#include<stdio.h>
#include "main.h"
/**
 * _strcat - the entry function
 * @dest: the first character that is checked
 * @src: the second character that is checked
 * Description: 'Program uses _strcat()'
 * Return: 0
 */
char *_strcat(char *dest, char *src)
{
return (0);
}
