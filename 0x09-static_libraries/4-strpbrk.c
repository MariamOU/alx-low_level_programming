#include<stdio.h>
#include "main.h"
/**
 * _strpbrk - the entry function
 * @s: the first character that is checked
 * @accept: the second character that is checked
 * Description: 'Program uses _strpbrk()'
 * Return: 0
 */
char *_strpbrk(char *s, char *accept)
{
return (0);
}
