#include<stdio.h>
#include "main.h"
/**
 * _strlen - the entry function
 * @s: the character that is checked
 * Description: 'Program uses _strlen()'
 * Return: 0
 */
int _strlen(char *s)
{
return (0);
}
