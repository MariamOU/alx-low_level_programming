#include<stdio.h>
#include "main.h"
/**
 * _strcpy - the entry function
 * @dest: the first character that is checked
 * @src: the second character that is checked
 * Description: 'Program uses _strcpy()'
 * Return: 0
 */
char *_strcpy(char *dest, char *src)
{
return (0);
}
