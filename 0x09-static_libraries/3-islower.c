#include<stdio.h>
#include "main.h"
/**
 * _islower - the entry function
 * @c: the character that is checked
 * Description: 'Program uses _islower()'
 * Return: 0
 */
int _islower(int c)
{
return (0);
}
