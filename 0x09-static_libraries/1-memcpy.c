#include<stdio.h>
#include "main.h"
/**
 * _memcpy - the entry function
 * @dest: the character that is checked
 * @src: the second character
 * @n: the third one
 * Description: 'Program uses _memcpy()'
 * Return: 0
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
return (0);
}
