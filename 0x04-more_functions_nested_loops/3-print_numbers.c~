#include<stdio.h>
#include "main.h"
/**
 * print_numbers : the entry function
 * Description: 'Program uses the function _putchar to print numbers (0 to 9)'
 * return : Always 0 (SUCCESS)
 */
void print_numbers(void)
{
int a;

for (a = 0; a <= 9; a++)
{
_putchar('0' + a);
}
}
